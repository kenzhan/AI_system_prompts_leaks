# TODO — Agent Customization Tasks

## Suggested Next Customizations

- [ ] `/create-prompt add-prompt` — A parameterized prompt that guides adding a new system prompt. Takes vendor name + model name as inputs, creates the `.md` file in the right folder, and patches the "Recently Updated" and vendor tables in `README.md` automatically.

- [ ] `/create-instructions` scoped to `**/README.md` — File-level instructions for maintaining README table format consistently when editing those files (column order, link format, date format).
